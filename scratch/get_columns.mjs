import { createClient } from '@supabase/supabase-js'; import 'dotenv/config'; const supabase = createClient(process.env.VITE_SUPABASE_URL, process.env.VITE_SUPABASE_ANON_KEY); async function run() { const { data: t } = await supabase.from('teams').select('code').limit(1); console.log('teams keys:', t && t.length > 0 ? Object.keys(t[0] || {}) : 'no data'); const { data: m } = await supabase.from('missions').select('team_id').limit(1); console.log('missions keys:', m && m.length > 0 ? Object.keys(m[0] || {}) : 'no data'); } run();
